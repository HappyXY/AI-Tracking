# 部署手册

本手册说明如何在本机部署 AI Tracking 每日抓取与自动提交流水线。  
仓库介绍与近期进展见根目录 [README.md](README.md)。

## 环境要求

- macOS（使用 Apple「邮件」App 读信）
- Python 3.10+
- Git，且已配置对 `HappyXY/AI-Tracking` 的 **SSH** 推送权限
- DeepSeek（或其它 OpenAI 兼容）API Key
- 已订阅 AINews、AlphaSignal，并落到同一邮箱

## 1. 克隆仓库

```bash
git clone git@github.com:HappyXY/AI-Tracking.git
cd AI-Tracking
```

若仍是 HTTPS 远程，请改为 SSH（避免每次输入密码）：

```bash
git remote set-url origin git@github.com:HappyXY/AI-Tracking.git
```

SSH 公钥在 `~/.ssh/id_rsa.pub`，需添加到 GitHub → Settings → SSH and GPG keys。

验证：

```bash
ssh -T git@github.com
```

## 2. 配置 Apple 邮件

1. 打开 **邮件** App  
2. **邮件 → 设置 → 账户** → 添加 **Outlook.com / Microsoft Exchange**  
3. 登录接收 AINews / AlphaSignal 的账号  
4. 等待收件箱同步出邮件  

> 不需要 Microsoft 365 / Outlook 桌面版。

## 3. Python 环境与密钥

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp agent/config.example.yaml agent/config.yaml
cp .env.example .env
```

编辑 `.env`（示例为 DeepSeek）：

```bash
OPENAI_API_KEY=sk-...
OPENAI_BASE_URL=https://api.deepseek.com/v1
OPENAI_MODEL=deepseek-v4-flash
```

`agent/config.yaml` 中默认：

```yaml
mail:
  backend: apple_mail   # Apple 邮件
schedule:
  hour: 11
  minute: 0
```

`.env`、`agent/config.yaml`、`agent/.state/` 已 gitignore，勿提交密钥。

## 4. 权限

首次运行时，系统可能提示授权控制「邮件」：

**系统设置 → 隐私与安全性 → 自动化** → 允许终端 / Cursor / `osascript` 控制 **Mail**。

## 5. 试跑与正式发布

```bash
# 只预览当月合并后的 Markdown，不写 git
./agent/run.sh --dry-run

# 写入 digest/YYYY-MM.md，并 commit + push
./agent/run.sh
```

产出文件形如：`digest/2026-07.md`（每月一个，按 LLM / VLM / Agent 等分类，按日累加）。

同日重复运行会覆盖当天条目；跨天会追加到当月文件。

## 6. 每日定时（launchd）

默认每天本地时间 **11:00**：

```bash
./agent/launchd/install_launchd.sh
```

- 日志：`~/Library/Logs/ai-tracking.log`  
- 停用：`launchctl unload ~/Library/LaunchAgents/com.happyxy.ai-tracking.plist`  
- 注意：到点时电脑需醒着；睡眠/关机不会自动补跑  

改时间：编辑 `agent/launchd/com.happyxy.ai-tracking.plist` 中的 `Hour` / `Minute`，再执行一次 `install_launchd.sh`。

## 7. 目录说明

```text
AI-Tracking/
  README.md                 # 面向读者的介绍与近期进展
  部署手册.md               # 本文件
  digest/YYYY-MM.md         # 每月进展（自动生成）
  agent/
    run.py / run.sh         # 入口
    apple_mail.py           # 读 Apple 邮件
    summarize.py            # LLM 总结
    monthly.py              # 按月合并
    publish.py              # git commit/push
    config.example.yaml
    launchd/                # macOS 定时
  .env.example
  requirements.txt
```

## 8. 可选后端

```yaml
mail:
  backend: apple_mail   # 默认（推荐）
  # backend: mac        # 经典 Outlook（需 M365 联机）
  # backend: graph      # Microsoft Graph（需自建 Entra 应用）
```

## 9. 常见问题

| 现象 | 处理 |
|------|------|
| 提示输入 GitHub 用户名密码 | 改用 SSH remote，并把本机公钥加到 GitHub |
| `Permission denied (publickey)` | 公钥未添加到当前 GitHub 账号，或 `ssh-add --apple-use-keychain ~/.ssh/id_rsa` |
| Mail inbox 为 0 | 账户未添加或未同步完 |
| 没有匹配邮件 | 检查发件人是否含 `ainews` / `alphasignal`；可调 `mail.lookback_hours` |
| 定时没跑 | 看日志；确认 11:00 时 Mac 醒着 |

## 10. 手动命令速查

```bash
./agent/run.sh --dry-run
./agent/run.sh --no-push      # 只本地 commit
./agent/run.sh --skip-git     # 只写 md 文件
./agent/run.sh --date 2026-07-30
```
